// Frontend configuration — works for both local dev and production
const CONFIG = {
    API_BASE: window.location.origin + '/api',
    GOOGLE_CLIENT_ID: '',
    APP_VERSION: '1.0.0',
    APP_NAME: 'Chess AI Coach',
};
